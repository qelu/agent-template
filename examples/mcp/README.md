# Optional MCP example

This example is deliberately excluded from initialized agents. Add an MCP only when the agent needs an external system integration and a selected runtime can enforce its permissions.

Before activation, define narrow tool schemas, separate reads from writes, keep credentials outside model context, set timeouts and output limits, and register the reviewed server as a proposed capability.
